#-Dspring.cloud.config.uri=http://IP:PORT
server:
  port: @project.service.port@

spring:
  application:
    name: orchestrator

#  cloud:
#    config:
#      failFast: true
#      name: Notifier
#      label: v1
#      username:
#      password: